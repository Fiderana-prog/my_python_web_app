version: '3.8'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      FLASK_KEY: 3456
      FLASK_ENV: development
      FLASK_APP: app.py   
    volumes:
      - .:/app
    restart: unless-stopped